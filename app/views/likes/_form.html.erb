<%= form_with(model: like, local: true) do |form| %>
  <% if like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
      <% like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :usuario_id %>
    <%= form.text_field :usuario_id, id: :like_usuario_id %>
  </div>

  <div class="field">
    <%= form.label :noticium_id %>
    <%= form.text_field :noticium_id, id: :like_noticium_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
